import React from 'react';
import { 
  AiOutlineInstagram 
} from 'react-icons/ai';

import {
  BsMoonFill
} from 'react-icons/bs';

import { Menu, MenuContainer, MenuItemGroup } from './styles';
import SideMenuItem from './SideMenuItem';

const SideMenu: React.FC = (): JSX.Element => {

  return (
    <Menu>
      <MenuContainer>
        <MenuItemGroup>
          oi
        </MenuItemGroup>
      </MenuContainer>
    </Menu>
  );
};

export default SideMenu;